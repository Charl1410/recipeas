import React from 'react'

import RecipeData from '../../types/index'

interface PopoverProps {
    change: string;
}


const PopOver: React.FC<RecipeData> =({change, data}) => {
  return (
    <div>
  
    </div>
  );
}

export default PopOver